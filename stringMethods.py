#Useful String Methods
name = "Calvin"

#len(name) -> length of string
#name.find("i") -> output: int,find the position of the char in str
#name.capitalize() -> makes first char into upper case
#name.upper() -> change all char in str into upper case
#name.lower() -> change all char in str into upper case
#name.isdigit() -> output: boolean, checks string is all int
#name.isalpha() -> output: boolean, checks string is all aplhabets
#name.count("i") -> output: int, counts number of char
#name.replace("i", "o") -> replaces existing char with a new char
